[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["python"]

[project]
name = "triqs_dftkit"
version = "@PROJECT_VERSION@"
authors = [
  { name="TRIQS", email="triqs@flatironinstitute.org" }
]
description = "DFT converters for TRIQS"
readme = "README.md"
keywords = ["triqs", "dft", "dmft"]
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: OS Independent",
]
dependencies = [
    "numpy",
    "scipy"
]

[project.urls]
"Homepage" = "https://triqs.github.io/dftkit"
"Bug Tracker" = "https://github.com/triqs/dftkit/issues"

# in case you want an executable hook of your app
# [project.scripts]
# solid_dmft = "solid_dmft.main:main"

